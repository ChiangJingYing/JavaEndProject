import static GameObject.GlobalParameter.SCREEN_HEIGHT;
import static GameObject.GlobalParameter.SCREEN_WIDTH;

public class Start {
    public static void main(String[] args) {
        new GameFrame("What's the tour", SCREEN_WIDTH, SCREEN_HEIGHT);
    }
}
